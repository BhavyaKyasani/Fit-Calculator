services:
  - type: web
    name: fit-calculator
    env: static
    buildCommand: npm install
    staticPublishPath: .
    routes:
      - type: rewrite
        source: /.*
        destination: /index.html
